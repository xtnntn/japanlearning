{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the personal daily reader.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
